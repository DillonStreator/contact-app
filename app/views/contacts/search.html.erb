<% @contacts.search(@search_name).each do |contact| %>
  <p>--------------------</p>
  <h2> <a href="/contacts/<%= contact.id %>"> <%= contact.last_name %>, <%= contact.first_name %></a></h2>
<% end %>